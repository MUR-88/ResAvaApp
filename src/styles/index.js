import { StyleSheet } from "react-native";

const globalStyles = new StyleSheet.create({
  textError: {
    color: "red",
    fontSize: 12,
    marginBottom: 12,
  },
  textWarning: {
    color: "#cc3300",
    fontSize: 12,
    marginBottom: 12,
  },
});

export { globalStyles };
